{
  "mcp_providers": {
    "exec": {
      "command": "/usr/local/bin/mcp-shell-server",
      "args": [],
      "env": {
        "ALLOW_COMMANDS": "dig,ping,nslookup,host,traceroute,curl,sosreport"
      },
      "tool_map": {
        "exec_argv": "shell_execute"
      }
    }
  }
}
